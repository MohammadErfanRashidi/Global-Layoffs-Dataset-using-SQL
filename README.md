# Global-Layoffs-Dataset-using-SQL
This project focuses on cleaning, standardizing, and analyzing a dataset of global layoffs using SQL. The raw dataset contained duplicates, inconsistent formatting, null values, and unstructured date fields. I designed a multi-step SQL pipeline that systematically transformed the dataset into a clean, reliable format.
